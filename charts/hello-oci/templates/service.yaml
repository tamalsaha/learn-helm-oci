apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-oci.fullname" . }}
  labels:
    {{- include "hello-oci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-oci.selectorLabels" . | nindent 4 }}
